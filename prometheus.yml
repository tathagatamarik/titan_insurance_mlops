global:
  scrape_interval: 10s

scrape_configs:
  - job_name: 'titan_insurance_api'
    static_configs:
      # host.docker.internal tells the Docker container to look at your Windows machine!
      - targets: ['host.docker.internal:5001']